# NOTE: ffmpeg is a system package (not a pip package).
# Install it via apt/yum or use install_server.sh / Dockerfile.
aiogram==3.31.0
yt-dlp==2026.8.19
gallery-dl==1.32.9
python-dotenv==1.2.3
aiosqlite==0.22.1
tzdata==2026.3
