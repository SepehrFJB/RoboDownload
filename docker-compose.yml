services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./downloads:/app/downloads
      - ./data:/app/data
      - ./backups:/app/backups
      - ./logs:/app/logs
      - ./assets:/app/assets
